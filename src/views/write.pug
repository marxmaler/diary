extend base.pug

block bodyBlock
    form(action="", method="post")
        div 
            input(type="text", name="title", placeholder="제목") 
        div
            textarea(name="content", cols="40", rows="20", placeholder="내용", style="resize:none") 
        button(type="submit") 저장